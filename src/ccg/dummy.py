"""In this module, we define a function add_values to add two values.
"""


def add_values(a: int, b: int) -> int:
    """add two values.

    Args:
        a (int): first value.
        b (int): second value.
    Returns:
        int: sum of the two values.
    """
    return a + b
